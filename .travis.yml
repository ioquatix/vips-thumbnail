os:
  - linux
#  - osx

sudo: false
dist: trusty

cache:
  - bundler

before_script:
  - gem update --system
  - gem install bundler

rvm:
  - 2.3
  - 2.4
  - 2.5
  - 2.6
  - ruby-head
  - rbx-2

env:
  - COVERAGE=true

matrix:
  allow_failures:
    - rvm: "rbx-2"
    - rvm: "ruby-head"
